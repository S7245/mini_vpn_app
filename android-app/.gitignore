# Gradle
.gradle/
build/
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
captures/

# Build products copied/generated by build-android.sh — NOT source of truth.
# The .so is cross-compiled from rust-core; the Kotlin bindings are generated
# by uniffi-bindgen. Both are regenerated, never hand-edited here.
app/src/main/jniLibs/
app/src/main/java/uniffi/
